import {AggridConfiguration} from "./AggridConfiguration";
import {StingerConfiguration} from "./StingerConfiguration";

export class GridConfiguration {
    aggrid: AggridConfiguration
    gridId?: string
    stinger: StingerConfiguration
}